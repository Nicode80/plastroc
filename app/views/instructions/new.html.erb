<div class="container">
  <div class="d-flex align-items-center justify-content-between">
    <h1>New instruction</h1>
  </div>
  <%= simple_form_for [@material, @instruction] do |f| %>
    <%= f.input :title, label: false, input_html: { class: "col-12 mb-3" }, placeholder: "Title" %>
    <%= f.input :content, label: false, input_html: { class: "col-12 mb-3" }, placeholder: "Content" %>
    <%= f.input :step_order, collection: (1..100), label: "select display order" %>
    <%= f.input :media, as: :file, label: false %>
    <div class='d-flex'>
      <%= f.submit "Create", class: "btn btn-primary rounded-pill d-block mt-3 plastroc-button plastroc-light-shadow mr-3" %>
      <%= link_to 'Cancel', material_path(@material), class: "btn btn-outline-primary rounded-pill d-block mt-3 plastroc-outline-button" %>
    </div>
  <% end %>
</div>
